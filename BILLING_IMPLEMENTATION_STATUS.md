# Montreal QBank billing implementation status

Last audited: 2026-08-27

Billing is implemented, disabled by default, and not yet safe for live payment collection. Live Stripe resources are staged, but owner verification, Canadian tax registration, replacement-secret deployment, approved paid content, staged enforcement, and Production verification remain.

## Phase status

| Phase | Status | Evidence and remaining work |
| --- | --- | --- |
| 0. Reproducible database | Implemented | GitHub `main` contains the complete ordered `supabase/migrations` history. README setup commands use tracked paths. Vercel reports Root Directory `.` for the repository rooted at `web/`. Local Supabase runtime metadata, environment files, source exports, and credentials are ignored. |
| 1. Stripe account and catalog | Test catalog verified; live catalog staged | Test and live products named `Montreal QBank` each have CA$59 monthly and CA$349 annual recurring CAD prices with no trial. The live prices are explicitly tax-exclusive, use distinct lookup keys, and share the explicit electronic-service tax code. The live customer portal supports payment-method updates, invoice history, and period-end cancellation. A least-privilege live restricted key is configured but cannot be saved until the owner completes Stripe identity verification. |
| 2. Billing data model | Implemented and applied | Billing migrations `0009_billing.sql` and `0010_billing_reconciliation.sql` are applied remotely with enforcement off. The complete ordered migration history through `0019_enforce_paid_content_approval.sql` is also applied. |
| 3. Server Stripe foundation | Implemented | Stripe, server-only Stripe and Supabase clients, configuration validation, trusted plan mapping, entitlement helpers, key-mode checks, approved-price enforcement during webhook synchronization, and disabled-by-default flags are present. |
| 4. Checkout, portal, and webhook | Complete test lifecycle verified; live endpoint staged | Authenticated Checkout and portal handlers, raw signature verification, atomic event claiming, idempotency, event ordering, failed-payment grace, paid-invoice recovery, cancellation access, and Stripe reconciliation are covered by tests. The protected Preview completed the full signed payment lifecycle. The active live webhook targets the canonical Production endpoint and listens to all six required events. Its initially displayed signing secret was expired immediately; saving the replacement secret to Vercel requires the pending owner verification. |
| 5. Entitlement enforcement | Implemented | Private layouts, paid data loaders, route handlers, and paid server actions enforce entitlement. Database RLS remains the final layer. Billing and Settings remain reachable for recovery, and demo mode bypasses live billing services. |
| 6. Billing interface | Implemented and published | Marketing pricing, Billing, Settings, portal recovery, bounded post-Checkout polling, mobile layout, accessibility, renewal and cancellation timing, approved refund terms, tax disclosure, legal links, operator identity, business address, public support email, and non-affiliation copy are present and verified on Production. |
| 7. Testing | Local gates and complete real test lifecycle pass | Lint, TypeScript, 89 unit tests, production build, 22 standard browser tests with 2 deliberate integration skips, 5 billing browser tests, and the dedicated full-stack rollback test pass. The protected Preview passes the 37-point readiness audit, signed delivery, duplicate-event idempotency, authenticated route smoke, Checkout branding, insufficient-funds decline, successful retry, webhook provisioning, portal inspection, stale-event rejection, authoritative reconciliation, failed renewal, exact three-day grace, paid recovery, period-end cancellation, and access cutoff. |
| 8. Safe rollout | Test rollout complete; live staging in progress | Billing enforcement is off in both the application and database. Live catalog, portal, prices, product tax metadata, and webhook endpoint are staged. Live price IDs are stored in Vercel Production. No live customer or payment exists. Owner verification, GST/HST and QST registration, replacement webhook secret, live key deployment, disabled-mode preflight, activation-time grants, staged enforcement, Production smoke, rollback rehearsal, and 48-hour monitoring remain. Production currently publishes zero rights-approved questions, so accepting payment would not satisfy the paid-content success criteria. |

## Current external evidence

- Billing readiness: the expanded secure Preview preflight passes 37 of 37 checks against the exact protected Preview origin.
- Database `billing_required` is false and `grace_days` is 3.
- Application `BILLING_REQUIRED` remains false in Vercel Production.
- Remote migrations `0001` through `0019` are applied and match the tracked local history.
- The dedicated billing-test account has one Stripe customer and one active scheduled-cancellation test subscription mirrored in Supabase. A separate 180-day Apple App Review grant expires on 2027-02-22. The webhook ledger includes the processed signed smoke and payment-lifecycle events without a reported processing error.
- Vercel contains Supabase variables, the Production canonical site URL, approved public prices and support email, completed legal terms, disabled billing safety flags, both test price IDs, the restricted Stripe test key, and the webhook signing secret in Preview and Development.
- Stripe CLI is not installed in the current development environment.
- Latest billing Preview: `https://lmcc-prep-p4sysjcrj-fuannes-projects.vercel.app`, deployment `dpl_3SsAfxrXUSgvDZL932v8fxXKrw5h`, built from the current verified tree after lifecycle completion and least-privilege cleanup. It is Ready with billing disabled, is the active Stripe test destination, and passes 37 of 37 readiness checks.
- Current Vercel Production deployment: `https://lmcc-prep-83rz8m7u6-fuannes-projects.vercel.app`, deployment `dpl_7tQRRKvDt9atiihAk14WFVmFxSV3`, built from an isolated archive of reviewed commit `549fb0b` and aliased to `https://lmcc-prep.vercel.app`. It is Ready with billing disabled. Concurrent uncommitted legal-page edits in the shared worktree were excluded.
- The canonical Production public and demo smoke suite ran after promotion on 2026-08-26: 21 tests passed, one mobile navigation check missed its five-second wait, and the 2 real-Supabase mutation checks remained intentionally opt-in. The single mobile check passed on immediate isolated rerun in 2.2 seconds. The suite verified the published operator identity, business address, public support email, prices, refund threshold, demo billing isolation, serious accessibility, mobile layout, PWA behavior, metadata, security headers, private-route controls, and zero approved public counts. The disabled capture endpoint remains covered by the route and header checks.
- The linked-database authorization verifier passed across 4,972 questions after the Production promotion on 2026-08-26. It confirmed equality between the published and approved public counts, answer-safe tags, anonymous isolation, service-only RPC protection, cross-user study-data protection, and cross-user billing-grant protection, then removed its temporary users and records.
- The dedicated rollback browser suite passed on 2026-08-25. It verifies that the documented database-first and application-second rollback state restores dashboard access and paid session creation for an invited user without a subscription.
- Stripe test product `prod_V8yyvR88zyshaR` has verified recurring CAD prices of CA$59 monthly and CA$349 annually, distinct lookup keys, and no trial. The active test customer portal enables invoice history, payment-method updates, and cancellation at the end of the billing period.
- Stripe live product `prod_V9924zR0KsQyGn` has live recurring CAD prices `price_1U8qkkCT7OeO0NwLKmlCZzMS` at CA$59 monthly and `price_1U8qkkCT7OeO0NwL1CCLGtSj` at CA$349 annually. Both prices are tax-exclusive, use distinct lookup keys, and have no trial. The product uses `MONTREAL QBANK` as its subscription statement descriptor and the explicit `txcd_10000000` electronic-service tax code.
- Live Stripe destination `we_1U8qyGCT7OeO0NwLcR4BFOvO` is active at `https://lmcc-prep.vercel.app/api/stripe/webhook`, uses API version `2022-11-15`, and subscribes to the six required events. The first signing secret was expired after accidental display during verification. The replacement secret is not yet stored in Vercel because Stripe requires owner identity verification before revealing it.
- Vercel Production contains the two approved live price IDs. `STRIPE_SECRET_KEY` remains absent, and `STRIPE_WEBHOOK_SECRET` must be replaced after owner verification. No deployment can create a live Checkout Session in this state.
- The restricted Stripe test key can read the approved catalog, invoices, portal, subscriptions, and webhook configuration and can perform only the application operations required for customers, Checkout, portal sessions, and readiness inspection. Temporary Subscriptions Write and Test Clocks Write permissions used for the isolated lifecycle were removed after testing; the saved key now has Subscriptions Read and Test Clocks None.
- The Vercel automation bypass is active. The protected Preview returns HTTP 401 without it and reaches the application webhook route with it, where an unsigned request correctly returns HTTP 400.
- Stripe test destination `we_1U8mBJCT7OeO0NwLCOQ7XGQk` is active against the latest protected Preview and listens to `checkout.session.completed`, the three customer subscription events, `invoice.paid`, and `invoice.payment_failed`. The signing secret is stored in Vercel Preview and Development.
- Direct signed delivery passed on 2026-08-26. Event `evt_mqbank_signed_smoke_1787773666376` returned HTTP 200 on first delivery and HTTP 200 with `duplicate: true` on the duplicate, while producing one processed ledger record and no billing state.
- Authenticated Preview route checks passed on 2026-08-26. An untrusted plan returned HTTP 400, the approved monthly plan returned a `checkout.stripe.com` URL, Manage billing returned a `billing.stripe.com` URL, and canceling Checkout returned to Billing with no charge or subscription.
- The same authenticated route smoke passed against the updated Preview after the destination change. Checkout displays `Montreal QBank` as the product heading and subscription title. Stripe's payment-authorization language still identifies the approved shared account as `Glass Railing - RF Transparent`.
- A real Stripe test-mode Checkout lifecycle completed on 2026-08-26. The insufficient-funds test card produced the expected safe decline message, retrying with Stripe's success card created one CA$59 monthly active subscription, and the signed webhook created exactly one active local subscription. The customer portal displayed the current plan, paid invoice, test payment method, next billing date, and period-end cancellation behavior.
- A separate isolated Test Clock lifecycle completed on 2026-08-26. Checkout used the approved monthly price and produced a paid invoice and active local access. The next renewal failed with Stripe's supported decline-after-attaching payment method, local status became `past_due`, and access ended exactly three days after the failure event. Stripe's scheduled retry then paid with a successful test method, restored active local status, and cleared the failure marker. Period-end cancellation remained entitled until the simulated boundary, then both Stripe and Supabase became canceled with access bounded to the paid period. The temporary auth user, Test Clock, customer, subscription, and local credential files were deleted after verification.
- Supabase auth state changed after the test lifecycle. A fresh private activation manifest now contains the owner and the existing Apple App Review account while excluding the billing-test account. Its dry run creates one non-expiring administrator grant and preserves the longer App Review grant through 2027-02-22. The previously listed additional invited account no longer exists, so the stale manifest must not be used.
- The first Checkout check displayed the shared account identity `Glass Railing - RF Transparent`; the signed-in Stripe Dashboard identifies the account as `RF-Transparent-Zoho` and shows account descriptor `GLASS-FENCE.COM`. On 2026-08-26, the owner explicitly approved using this shared account. The implementation now applies a per-Checkout `Montreal QBank` display name, and the Montreal QBank product will use its own `MONTREAL QBANK` subscription statement descriptor without changing account-wide details.
- The owner approved `15041074 Canada Inc.` and its Etobicoke business address for public legal disclosure, and privately supplied the Canadian GST/HST and Quebec QST registrations for later Stripe validation. Registration numbers are not stored in source control.
- The owner approved `fuanne_gm@hotmail.com` as the public support email.
- The application, billing plan, and readiness preflight consistently use the customer-facing name `Montreal QBank`. Infrastructure identifiers remain unchanged.
- GitHub `main` contains the complete billing implementation and tracked migration history.

## Safe next sequence

1. Complete Stripe owner verification, save the live restricted key, reveal the rotated webhook signing secret, and replace the Production secret in Vercel.
2. Add the existing GST/HST and Quebec QST registrations, verify Stripe Tax, and enable automatic tax only after the registrations are active.
3. Deploy and run the complete live readiness preflight with both enforcement switches off.
4. Do not accept payment while the production approved-content aggregate remains zero.
5. At an approved activation timestamp, apply the refreshed complimentary-grant manifest, preserving the App Review grant.
6. Deploy application enforcement first, verify health, then enable database enforcement.
7. Run Production payment, access, smoke, and rollback checks, then monitor the first 48 hours.
